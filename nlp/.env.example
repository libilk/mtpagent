# RAG 项目环境变量配置模板
# 复制此文件为 .env 并填入你的 API Key
# ================================

# 通义千问 API Key（必填，用于 LLM 调用和 Embedding）
# 获取地址: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=sk-your-dashscope-api-key

# Redis 连接地址（可选，用于缓存持久化，不配置则自动降级为纯内存缓存）
# 本地开发: redis://localhost:6379/0
# 带密码:   redis://:your-password@localhost:6379/0
# REDIS_URL=redis://localhost:6379/0

# Tavily 网络搜索 API Key（可选，用于 knowledge_agent web_search 工具）
# 获取地址: https://app.tavily.com/
# TAVILY_API_KEY=tvly-your-api-key

# ================================
# LangSmith 链路追踪（可选，默认关闭）
# ================================
# 用途：把一次请求的每个节点 / 每次 LLM 调用 / 每次工具调用上报成一棵带耗时归因的
# span 树，用来定位"这几秒花在哪一步"。项目原有的自建 trace 只有扁平的进度事件。
#
# ⚠️ 隐私：开启后【完整 prompt、用户原话、工具返回结果】都会上传到 LangSmith 云端。
#    本项目有「不长期留存投诉原文」的数据红线（study.md §7.1），
#    生产环境请先脱敏，或打开下面的 HIDE 开关只保留耗时结构。
#
# 获取 API Key: https://smith.langchain.com  →  Settings → API Keys
# 启用方式：把下面三行的注释去掉并填上值
# LANGSMITH_TRACING=true
# LANGSMITH_API_KEY=lsv2_pt_your-langsmith-api-key
# LANGSMITH_PROJECT=mtpagent-nlp

# 只上报结构、不上报内容（想开追踪又不想把用户原话传出去时用这两个）
# LANGSMITH_HIDE_INPUTS=true
# LANGSMITH_HIDE_OUTPUTS=true
